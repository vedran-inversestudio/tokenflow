{
  "name": "@tokenflow/bridge",
  "version": "1.0.0",
  "description": "Bridge server for Tokenflow token communication",
  "main": "server.cjs",
  "scripts": {
    "start": "node server.cjs",
    "dev": "nodemon server.cjs",
    "test": "echo \"No tests specified\" && exit 0",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "bridge-server",
    "token-communication",
    "websocket"
  ]
}
